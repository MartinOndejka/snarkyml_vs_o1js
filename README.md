# Init

To have access to mina dependencies, clone into the mina repo in `src/app/zkapps_examples/`.

# Run ocaml

```bash
cd snarkyml
dune exec ./run.exe
```

# Run o1js

```bash
cd o1js
npm install
npm run start
```
